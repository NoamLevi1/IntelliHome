@{
    ViewData["Title"] = "Home Page";
    ViewData["DisableHeader"] = true;
}

<div>
    <div class="d-flex align-items-center justify-content-center">
        <img 
             src="Images/Transparent Logo-cropped.png"
             width="50%"
             height="auto"
             alt="" />
        </div>
    <h1 class="d-flex align-items-center justify-content-center pb-4"> Welcome!</h1>
    <div class="d-flex align-items-center justify-content-center">
        @Html.ActionLink("Login", "Login","Account" ,null,new {@class="btn btn-primary"})
    </div>
</div>